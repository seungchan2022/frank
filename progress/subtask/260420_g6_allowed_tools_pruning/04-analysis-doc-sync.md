# 서브태스크 04 — 분석 문서 before/after 표 추가 + 3곳 ⏳→✅ 동기화

> 생성일: 260420
> 메인태스크: `progress/260420_g6_allowed_tools_pruning.md`
> 상태: 📝 계획 (step-4 대기)
> 의존성: **서브태스크 01, 02, 03 모두 완료 후** (모든 변경 반영 필요)
> 예상 소요: 30~45분
> 커밋 분류: `docs:` (feat 커밋과 분리)

## 목적

`progress/analysis/260417_하네스_비교분석.md`에 G6 결과 반영:
1. **before/after 비교 표** — 6개 스킬 × (`allowed-tools` 라인 수 + MCP 서버 수) 추가
2. **⏳ → ✅ 태그 동기화** — 요약 표 G6 / §5 G6 / §7 우선순위표 2번 총 3곳

사용자 Q1 답 "A=간이 지표만"에 따라 라인 수 + MCP 서버 수만 기록. 토큰 실측은 **수행하지 않음**.

## 산출물

- `progress/analysis/260417_하네스_비교분석.md` 내 변경:
  - before/after 비교 표 신설 (6행 × 5열: 스킬명, before 라인, after 라인, before MCP 수, after MCP 수)
  - 요약 표에서 G6 상태 ⏳ → ✅
  - §5 G6 섹션 상태 ⏳ → ✅ + 실제 적용 결과 요약 3~4줄
  - §7 우선순위표 2번 행 상태 ⏳ → ✅
- (선택) HTML 버전 동기화: `260417_하네스_비교분석.html` — MD 기반으로 재생성 가능하면 재생성, 아니면 수동 업데이트는 생략
- `progress/260420_g6_allowed_tools_pruning.md` 상태 업데이트: `📝 계획` → `✅ 완료`

## 작업 단계

1. **수치 수집**: 서브태스크 01/02의 before/after 결과를 기반으로 6행 표 데이터 준비
   - 라인 수: `allowed-tools` 블록 내 YAML 리스트 항목 개수
   - MCP 서버 수: `mcp__{server}__{tool}` 접두사 고유 서버 수
2. **before/after 표 위치 결정**: §5 G6 섹션 하단 또는 §8 실행 기록 섹션에 신설
3. **§5 G6 본문 수정**: 상태 ⏳ → ✅ + 적용 결과 3~4줄 (어떤 스킬에서 몇 개 제거했는지)
4. **§7 우선순위표 2번 동기화**: 상태 컬럼 ⏳ → ✅
5. **요약 표 동기화**: 문서 상단 요약 표에서 G6 행 상태 업데이트
6. **메인 태스크 문서 상태 갱신**: `260420_g6_allowed_tools_pruning.md` 헤더 상태 변경
7. **커밋 분리 확인**: feat(스킬 frontmatter + agents.md) 커밋과 docs(분석 문서 동기화) 커밋 분리 (`feedback_commit_granularity`)

## 완료 조건

- [ ] 비교 표 추가됨 (6행, 모든 셀 값 있음)
- [ ] 요약 표 G6 행 ✅
- [ ] §5 G6 섹션 상태 ✅ + 적용 결과 요약
- [ ] §7 우선순위표 2번 ✅
- [ ] 메인 태스크 문서 상태 `✅ 완료`
- [ ] docs 커밋과 feat 커밋이 분리됨 (step-9에서 확인)

## 리스크

- **숫자 불일치**: 서브태스크 01/02에서 수정한 파일의 현재 상태와 표 값이 어긋나면 문서 신뢰도 하락 → 표 작성 시 다시 `head -30 SKILL.md | grep -c` 방식으로 재확인
- **HTML/MD 불일치**: HTML은 MD에서 생성되는 것이 아니라 수동 관리 → 이번 사이클은 MD만 업데이트 (HTML 동기화는 별도 논의)

## 다음 태스크

→ `/step-5` 리뷰 → `/step-6` 구현 → `/step-7` 리팩토링 → `/step-8` 테스트 → `/step-9` 커밋
