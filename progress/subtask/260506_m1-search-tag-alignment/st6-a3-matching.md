# ST-6: A3 매칭 전략 구현

> 유형: feature | 의존: ST-1 | 예상: 2h

## 목적

ST-1 진단 결과를 반영해 태그-기사 매칭 전략을 구현한다.

## A3 운영 정의 (step-3 확정)

**(i) 통계 안정**: 각 태그 쿼리를 정밀화해 해당 태그와 관련된 기사만 반환. 모델 변경 없음 (`FeedItemResponse.tag_id: Option<Uuid>` 유지).

## ST-1 진단 결과별 구현

> 케이스 C(사후 분류)는 A3 운영 정의에서 기각. 이 서브태스크에서 고려하지 않음.

### 케이스 A: 쿼리 정밀화(ST-2/ST-3)로 충분

추가 구현 불필요. ST-6 스킵 — 완료 기준에 "케이스 A 확인됨, ST-6 스킵" 주석 기록.

### 케이스 B: 통합 캐시 전략 필요

태그별 검색 결과를 캐시로 공유해 동일 검색 두 번 실행 시 일관된 결과 반환.

```rust
// make_cache_key() 확장:
// - 태그별 캐시: "{user_id}:{tag_id}" (기존)
// - 결과 공유: 동일 user_id + 짧은 TTL(5분) 내 동일 쿼리 캐시 히트
```

## 응답 스키마 계약 (M4 보호)

`FeedItemResponse`의 `tag_id: Option<Uuid>` 필드는 변경하지 않는다.
M4 iOS가 의존하는 필드이므로 이번 M1에서 스키마 변경 금지.

## 완료 기준

- [ ] ST-1 결정 기록 참조
- [ ] 케이스에 따른 구현 (또는 스킵 기록)
- [ ] `tag_id` 응답 필드 불변 확인
- [ ] 단위 테스트 통과
