# 하네스 분석 문서 G1 관련 3곳 동기화

> 상위: 260420_g1_feature_list_skill
> 번호: 04
> 상태: 대기
> 유형: 문서업데이트

---

## 스코프

### In-Scope
- `progress/analysis/260417_하네스_비교분석.md` 갱신:
  1. 최상단 최종 갱신 일자: `2026-04-20 → 2026-04-21`(또는 완료일)
  2. 최신 갱신 요약 표 G1 행: ⏳ 구현 예정 → ✅ 해결 (**§8.2 행은 현행 유지**)
  3. §5 G1 본문: 상태/방법 ⏳ → ✅
  4. §7 우선순위표 G1 행: ⏳ → ✅ (**§8.2 행은 현행 유지**)
- **커밋 타이밍**: 01·02 feat 커밋 + E2E 검증 완료 후 **docs 단독 커밋 1회**로 일괄 동기화
- 커밋 전제 조건 (critical-review 지적 반영):
  - 01·02가 **같은 feature 브랜치 내 연속 작업**이고 **외부 공유 전**인 경우에만 docs 후행 단독 커밋 허용
  - 중간에 리뷰/머지 발생하면 해당 feat 커밋에 docs 동기화를 묶어 분리

### Out-of-Scope
- **§8.2 관련 표시 변경** — §8.2는 본 G1 사이클에서 분리되어 별도 후속 태스크로 이관됨 (03 문서 상단 이관 안내 참조). 실행 순서표 `G1 → G6 → 8.2`도 **현행 유지**
- 기타 하네스 문서 전반 리팩터링

---

## Definition of Done (DoD)

### 코드 품질
- (해당 없음 — 문서 전용)

### 문서/프로세스
- [ ] 4곳 모두 ✅로 갱신됨
- [ ] 최종 갱신 일자 갱신
- [ ] `docs:` 태그로 단독 커밋
- [ ] 사용자 승인

---

## 상세 구현 명세

### 변경 파일
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `progress/analysis/260417_하네스_비교분석.md` | 수정 | G1 관련 4곳 ⏳ → ✅ 동기화 |

### 구현 상세
1. **상태 필드 전용 앵커로 대상 위치 확정** (critical-review 지적 반영 — 본문 `G1` 언급은 무시):
   - 요약 표 G1 상태 열 (파일 상단 근처)
   - `### 🥇 G1.` H3 섹션의 상태 태그 라인
   - §7 우선순위표 G1 행의 상태 열
   - 최상단 최종 갱신 일자
2. 각 위치 Edit (요약 표 → §5 → §7 → 최상단 순)
3. 검증: `rg -n "G1.*⏳"` 결과가 0건이며 `G1.*✅` 3곳 확인
4. 커밋 메시지:
   ```
   docs: 하네스 분석 G1 상태 ✅로 동기화

   - 요약 표 · §5 G1 · §7 우선순위표 · 최상단 갱신일 동기화
   - step-4/8 스킬 구현 완료 반영 (§8.2는 별도 후속 태스크로 이관)
   - 다음 최우선: G6 → 8.2
   ```

---

## 테스트 계획
| 테스트 | 설명 | 유형 |
|--------|------|------|
| 상태 필드 Grep | `rg -n "G1.*⏳"` → 0건 확인 + `rg -n "G1.*✅"` → 3곳 확인 (본문 언급 분리) | 수동 |
| §8.2 현행 유지 확인 | `rg -n "8\\.2.*⏳"` → 기존 건수 유지 (감소 금지) | 수동 |
| 실행 순서표 유지 | 507~509 `G1 → G6 → 8.2` 행 내용 변경 없음 확인 | 수동 |
| 시각 검토 | 렌더링된 마크다운 테이블 정렬 유지 확인 | 수동 |

---

## TDD 예외 사유 (`0_CODEX_RULES.md §8`)

문서만 수정. 코드 동작 변경 없음 → TDD 생략.

---

## 리뷰 결과

### Claude 리뷰 (문서 일치성)
- **동기화 범위가 03 결정에 종속**: 03을 G1에 유지하면 §8.2 영역도 같이 갱신해야 하지만 04 Out-of-Scope는 "8.2 관련 표시 변경 금지"로 고정. 03 의사결정 전에는 04 스코프 확정 불가.
- **Grep 검증 `⏳.*G1` 약함**: G1은 본문/실행 순서/요약 표 등 상태 필드 외 맥락에도 등장. 상태 필드 전용 고정 앵커 필요.

### Codex 리뷰 (기술적 타당성)
- ⚠️ **조건부 승인**
- 4곳 동기화 기준이 현재 분석 문서 구조와 맞지 않음 (상태성 G1 언급은 4곳이지만 실행 순서·8.2 관계 설명도 얽힘)
- 검증 계획 `⏳.*G1 = 0건`은 약함 → 상태 필드 전용 앵커 검증 (`rg -n "^### 🥇 G1\\."`, `요약 표 G1 행` 등)
- `feat 다 끝난 뒤 docs 단독 커밋 1회`는 외부 공유 전일 때만 안전. 공유 흐름이면 stale 기간 발생 → 안전 조건 문서화 필요

### 구멍 찾기 리뷰 (critical-review)
- 03과 공유되는 치명: "03이 8.2 선점 → 04가 G1 ✅ 찍으면 8.2 미완료인데 완료로 위장". 04 단독으로는 해결 불가, 03 재범위 결정과 연동.

### 최종 결정: ⚠️ **조건부 승인** — 03 의사결정 후 범위 확장 결정
반영 필수:
1. 03 결정(A/B)에 따라 04 대상 확장 여부 결정
   - A안(03 유지): §8.2 상태 문구 + 실행 순서표까지 동기화 대상 확장
   - B안(03 분리): 현재 4곳 유지 + 03이 8.2 별도 태스크로 이관됐다는 메모 추가
2. 검증 계획을 상태 필드 전용 앵커로 교체 (`rg -n "^### 🥇 G1\\."` 등)
3. 커밋 전략에 "외부 공유 전 같은 feature branch 내 연속 작업 시에만 docs 후행 단독 커밋 허용" 조건 명시
