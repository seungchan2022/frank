# M4: 클라이언트 표시·회귀

> 프로젝트: Frank MVP16
> 상태: 대기
> 예상 기간: 3~5일
> 의존성: M1 (응답 스키마 박제 후 진입)

## 목표

앱·웹 표시 일관성 회복 + 실기기 회귀 복원 + 오답 노트 원문 이동 추가. iOS 카드 태그 표시, 실기기 오답 저장, 오답노트 태그 필터, 오답 노트 원문 이동까지 4건 모두 복구.

## 배경 (`_review_notes.md` §2 클라이언트: 표시·회귀)

실사용 피드백에서 다음 4건이 이 마일스톤에 묶인다 — E1(앱 피드 카드에 기사 태그 미표시), F1(실기기에서만 오답 저장 안 됨), F2(앱 오답노트 태그 필터 미동작), D2(오답 노트 원문 URL 이동 불가 — M3에서 재배치).

## 포함 항목

### E1. 앱 피드 카드에 기사 태그 미표시

- **상황**: 앱(iOS) 전체 피드 화면에서 카드 보기
- **현재**: 카드에 태그가 표시 안 됨. 웹은 카드에 태그 표시됨 (웹 ↔ 앱 싱크 불일치). 사용자 메모: "전체 피드에서 태그별로 나눠서 표현하고 있는데" — 카드에 태그가 보여야 분류 인지 가능.
- **기대**: iOS 카드에 태그 라벨/칩 표시 (웹과 일관)
- **수정 결**: 서버 응답에 `article.tags` 존재 여부 확인 → 누락이면 추가. iOS Feed 카드 뷰에 태그 칩 렌더링 추가.

### F1. 실기기에서만 오답 저장 안 됨

- **상황**: iOS 시뮬레이터에서 오답 저장 → 정상. 동일 시나리오를 실제 iPhone에서 → 피드 오답보기/즐겨찾기/오답노트 어디에도 안 나타남.
- **현재**: 환경 분기 회귀. 시뮬레이터/실기기 차이가 의심됨 — Keychain 읽기, 인증 토큰 만료, API base URL 분기, 또는 ATS 정책.
- **기대**: 실기기에서도 시뮬레이터와 동일 동작
- **수정 결**: **step-6에서 진단 필수** — 실기기 로그/네트워크 응답 확인, 토큰/Keychain 동작 확인. 원인 확정 후 픽스.

### F2. 앱 오답노트 태그 필터 미동작

- **상황**: 앱 오답노트 탭에서 태그 필터 칩 클릭. 이전 MVP에서 동작했던 기능.
- **현재**: 필터링 결과가 변하지 않음. 클라이언트 로직 회귀인지 서버 응답 변화인지 불명.
- **기대**: 태그 칩 클릭 시 해당 태그 오답만 노출
- **수정 결**: **step-6에서 진단** — git log/blame으로 마지막 동작 시점 확인 + 현재 코드 흐름 추적. 필터 로직 복원.

### D2. 오답 노트에 원문 URL 이동 불가 (M3에서 재배치)

- **상황**: 사용자가 퀴즈 오답을 저장 → 오답노트 탭에서 그 오답 클릭
- **현재**: 원문 기사 이동 불가. 서버 측은 이미 정합 — `quiz_wrong_answers.article_url` 컬럼 존재 + 저장 페이로드도 article_url을 받음 (`server/src/api/quiz_wrong_answers.rs:32`, `domain/models.rs:76`). 클라이언트의 오답 카드/디테일 뷰에 원문 이동 액션이 없을 뿐.
- **기대**: 오답 클릭 시 원문 기사로 이동 가능
- **수정 결**: 클라이언트 UI 추가 — iOS 오답노트 + 웹 오답노트에 원문 이동 버튼/탭 추가. **이동 방식(새 탭 vs 인앱 웹뷰 vs 기사 디테일 화면 재진입)은 step-1에서 결정**.

## 미결정 (워크플로우에서 결정)

| 항목 | 결정 시점 | 비고 |
|------|-----------|------|
| F1 실기기 오답 저장 회귀 원인 (Keychain? 토큰? URL? ATS?) | step-6 진단 | |
| F2 오답노트 태그 필터 회귀 원인 (클라이언트? 서버 응답?) | step-6 진단 | |
| D2 오답 → 원문 이동 UI (새 탭 vs 인앱 웹뷰 vs 기사 디테일 재진입) | step-1 | 웹/iOS 양쪽에 일관 적용 |

## 성공 기준 (Definition of Done)

- [ ] E1: 서버 응답에 `article.tags` 필드 존재 확인 (M1 변경과 정합 — 응답 스키마 박제)
- [ ] E1: iOS Feed 카드 뷰에 태그 칩 렌더링 추가 + ViewInspector 또는 스냅샷 테스트
- [ ] F1: 진단 보고서 작성 (`progress/mvp16/F1_diagnosis.md` 또는 PR 본문)
- [ ] F1: 원인 확정 후 픽스 + 단위 테스트
- [ ] F2: 진단 결과 (클라이언트 vs 서버) 명시 + 픽스 + 단위 테스트
- [ ] D2: 오답 클릭 → 원문 이동 UI 구현 (step-1 결정 사항 반영, 웹+iOS)
- [ ] D2: 기존 오답(article_url 비어있는 행) 표시 분기 — 이동 비활성/숨김
- [ ] 본인 직접 사용 (시뮬레이터): iOS 피드 카드에 태그 칩 표시 확인 (E2E)
- [ ] 본인 직접 사용 (**실기기**): 오답 저장 → 즐겨찾기/오답노트 표시 확인 (E2E) — `feedback_e2e_within_workflow` 정합
- [ ] 본인 직접 사용 (시뮬레이터): 오답노트 탭에서 태그 칩 클릭 → 필터링 동작 확인 (E2E)
- [ ] 본인 직접 사용 (시뮬레이터+웹): 오답 클릭 → 원문 이동 확인 (E2E)
- [ ] iOS 단위·UI 테스트 통과 (`xcodebuild test -workspace Frank.xcworkspace -scheme Frank`)
- [ ] iOS coverage 게이트 통과 (`scripts/coverage.sh` 90% 이상)

## 워크플로우 진입점

```
/workflow "M4-client-display-regression"
```

**메인태스크**: iOS 피드 카드 태그 칩 표시 + 실기기 오답 저장 회귀 진단·복원 + 오답노트 태그 필터 회귀 진단·복원 + 오답 → 원문 이동 UI 추가.

## 아이템

| # | 아이템 | 유형 | 순서 | 상태 |
|---|--------|------|------|------|
| 1 | D2 원문 이동 UI 결정 (새 탭 vs 인앱 웹뷰 vs 기사 디테일 재진입) | decision | 1 | 대기 |
| 2 | F1 실기기 오답 저장 회귀 진단 (Keychain/토큰/URL/ATS) | research | 2 | 대기 |
| 3 | F2 오답노트 태그 필터 회귀 진단 (git blame + 코드 흐름) | research | 3 | 대기 |
| 4 | E1 iOS 피드 카드 태그 칩 렌더링 (서버 응답 의존) | feature | 4 | 대기 |
| 5 | F1 픽스 (2번 진단 결과 반영) | feature | 5 | 대기 |
| 6 | F2 픽스 (3번 진단 결과 반영) | feature | 6 | 대기 |
| 7 | D2 원문 이동 UI 구현 (1번 결정 반영, iOS + 웹) | feature | 7 | 대기 |
| 8 | E2E 시나리오 4종 통과 (실기기 1건 포함) | chore | 8 | 대기 |

## KPI (M4)

| 지표 | 측정 방법 | 목표 | 게이트 | 기준선 |
|---|---|---|---|---|
| iOS 테스트 통과 | `xcodebuild test -workspace Frank.xcworkspace -scheme Frank -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` | 전체 통과 | Hard | — |
| 웹 테스트 통과 | `npm run test` (web) | 전체 통과 | Hard | — |
| iOS 카드 태그 칩 표시 회귀 차단 | 본인 E2E (시뮬레이터): 피드 카드 태그 칩 노출 | 통과 | Hard | 미표시(버그) |
| 실기기 오답 저장 회귀 차단 | 본인 E2E (**실기기**): 오답 저장 → 즐겨찾기 노출 | 통과 | Hard | 미저장(버그) |
| 오답노트 태그 필터 회귀 차단 | 본인 E2E (시뮬레이터): 태그 칩 클릭 → 필터링 동작 | 통과 | Hard | 미동작(버그) |
| 오답 → 원문 이동 회귀 차단 | 본인 E2E (시뮬레이터+웹): 오답 클릭 → 원문 도달 | 통과 | Hard | 이동 불가(버그) |
| F1 진단 보고서 작성 | `progress/mvp16/F1_diagnosis.md` 또는 PR 본문 | exists | Hard | — |
| iOS 테스트 커버리지 | `scripts/coverage.sh` (Frank 타겟 lineCoverage) | ≥85% | Soft | MVP15 종료 시 값 |

## 리스크

| 리스크 | 영향(H/M/L) | 대응 |
|--------|------------|------|
| F1 원인이 ATS 또는 Keychain access group → 빌드 설정 변경 필요 | M | 진단 보고서로 영향 범위 명시. 빌드 설정 변경 시 dev/prod 분기 보존 |
| F2 회귀 원인이 서버 응답 형태 변경 → 서버 마일스톤(M1)으로 역류 | M | 진단 후 서버 변경이 필요하면 M1으로 역류시키거나 M4 안에서 서버 작업 포함 결정. 의존 명시 |
| E1이 M1 응답 스키마 변경에 의존 → M1 변경 후 재작업 가능성 | M | M1 step-1에서 `article.tags` 필드 형태를 박제. M4는 그 형태에 맞춰 구현. 변경 발생 시 M1 안에서 흡수 |
| 실기기 E2E를 미루고 시뮬레이터 통과로 done 처리 | H | DoD에 "**실기기**" 명시. `feedback_e2e_within_workflow` 정합. 실기기 통과 전 done 차단 |
| D2 원문 이동 UI가 인앱 웹뷰 선택 시 ATS·세션 전달 이슈 발생 | M | step-1 결정 시 인앱 웹뷰 선택하면 SFSafariViewController 우선. 새 탭(외부 브라우저) 선택이 가장 안전 |

## 참고

- 합의 노트: `progress/mvp16/_review_notes.md` §2.4 (클라이언트: 표시·회귀)
- 코드: `server/src/api/quiz_wrong_answers.rs:32`, `server/src/domain/models.rs:76` (D2 article_url 컬럼·페이로드 이미 존재)
- 관련 메모리: `feedback_e2e_within_workflow`, `feedback_e2e_before_commit`, `feedback_swift_lsp_ban`, `reference_xcodebuild_workspace`
