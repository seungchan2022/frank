# M3: 온보딩

> 생성일: 260406
> 완료일: 260406
> 상태: ✅ 완료
> 브랜치: feature/m3-onboarding → main 머지 완료

## 요구사항 (인터뷰 합의)

| 항목 | 결정 |
|------|------|
| 온보딩 완료 후 전환 | checkSession 재호출 (서버 Profile 재조회) |
| 태그 선택 UI | 플로우 레이아웃 (칩 가로 줄바꿈) |
| 최소 선택 개수 | 1개 이상 |
| 서버 업데이트 방식 | user_metadata 업데이트 (M6에서 profiles 테이블 마이그레이션 예정) |
| 화면 구성 | 단일 화면 (타이틀 + 태그 칩 + 하단 CTA) |
| TagChip 디자인 | 채움/아웃라인 토글 (선택: tint+흰글자, 미선택: 아웃라인+회색) |
| CTA 버튼 위치 | SafeArea 하단 고정, 1개 이상 선택 시 활성화 |
| 로딩/에러 처리 | 버튼 내 ProgressView + 인라인 에러 메시지 |
| 패턴 | 에코 서버 (TagPort + AuthPort → Mock 주입 TDD) |

## 서브태스크

### ST-0: AuthPort 확장 + Mock 갱신
- **유형**: core
- **상태**: [x]
- **설명**: AuthPort에 `updateOnboardingCompleted() -> Profile` 추가. MockAuthPort/SupabaseAuthAdapter 구현.
- **산출물**: AuthPort.swift, MockAuthPort.swift, SupabaseAuthAdapter.swift, PortContractTests.swift

### ST-1: OnboardingFeature 구현 (TDD)
- **유형**: feature
- **상태**: [x]
- **설명**: @Observable, 상태: loading/loaded(tags,selectedIds). 별도 프로퍼티: isSaving, errorMessage. Action: loadTags, toggleTag, complete. 저장 실패 시 태그 선택 유지.
- **산출물**: OnboardingFeature.swift, OnboardingFeatureTests.swift (13개 테스트)

### ST-2: TagChipView + FlowLayout 컴포넌트
- **유형**: component
- **상태**: [x]
- **설명**: 재사용 순수 UI. 채움/아웃라인 토글. Layout 프로토콜 기반 FlowLayout.
- **산출물**: Components/TagChipView.swift

### ST-3: OnboardingView UI
- **유형**: feature
- **상태**: [x]
- **설명**: 타이틀 + FlowLayout 태그 칩 + 하단 CTA. isSaving 시 터치 차단 + 버튼 ProgressView. errorMessage 인라인 표시.
- **산출물**: Features/Onboarding/OnboardingView.swift

### ST-4: SupabaseTagAdapter 구현
- **유형**: adapter
- **상태**: [x]
- **설명**: TagPort 프로덕션 구현체. tags/user_tags 테이블 REST API. DTO 매핑. user_id 명시적 필터.
- **산출물**: Core/Adapters/SupabaseTagAdapter.swift

### ST-5: RootView 온보딩 분기 + AppDependencies 연결
- **유형**: integration
- **상태**: [x]
- **설명**: profile.onboardingCompleted 분기. OnboardingContainerView로 Feature 라이프사이클 관리. PlaceholderTagPort → SupabaseTagAdapter 교체. onComplete → checkSession 재호출.
- **산출물**: FrankApp.swift, AppDependencies.swift

## 리팩토링 내역

| 이슈 | 수정 |
|------|------|
| saving/error 상태에서 태그 데이터 유실 | state를 loading/loaded만으로 축소, isSaving/errorMessage 별도 프로퍼티 분리 |
| previousLoadedState 버그 (항상 현재 state 반환) | 삭제 — 불필요 |
| 인라인 에러 UI 죽은 코드 | feature.errorMessage에서 직접 읽도록 수정 |
| fetchMyTagIds 사용자 필터 누락 | .eq("user_id", value: userId) 명시적 필터 추가 |

## 테스트 결과

- 55개 단위 테스트 전체 통과 (6 suites)
- OnboardingFeature 커버리지: 100% (66/66)
- AuthFeature 커버리지: 98.75% (79/80)

## E2E 검증 (260406)

- UITest 자동화로 전체 플로우 검증 완료 (e2e@frank.dev)
- 로그인 → 온보딩 분기 → 12개 태그 로드 → FlowLayout + 하단 CTA 정상
- 앱 재실행 시 세션 복원 → 온보딩 화면 바로 표시 확인
- Tag 모델 slug → category 수정 (DB 스키마 일치)

## 기술 부채

- user_metadata에 onboardingCompleted 저장 → M6에서 profiles 테이블 마이그레이션 예정
- saveMyTags의 delete → insert 비원자적 처리 → 후속 RPC 함수 도입 고려
