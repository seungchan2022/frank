# MVP 1 기획

> 생성일: 260404
> 완료일: 260405
> 상태: **완료** (2일 만에 기획~배포 전 과정 완주)

## 요약

AI 기반 뉴스 스크랩 스터디앱의 MVP 1 기획.
키워드/태그 선택 → 자동 웹서치 → LLM 요약+인사이트 제공.

## 최종 결과

| 항목 | 수치 |
|------|------|
| 개발 기간 | 2일 (기획 1일 + 구현 1일) |
| 커밋 | 32개 (feat 10, fix 8, chore 6, docs 5, refactor 1, test 1) |
| 코드 | 5,426줄 (서버 3,365 + 웹 2,061) |
| 테스트 | 135개 전체 통과 (서버 69 + 웹 66) |
| 외부 API | 6개 (Tavily, Exa, Firecrawl, OpenRouter, Supabase, iMessage) |
| 포트/어댑터 | 5 포트, 5 Fake 어댑터 |
| 기술 부채 | 23건 (Critical 3, Major 12, Minor 8) |

> 상세 회고: `progress/retro/260405_mvp1_completion_retro.md` 참조

## 상세 내용

### 핵심 가치

AI 정보 과잉 문제를 해결 — 개인화 키워드 기반 자동 수집 + LLM 인사이트 제공.

### 아키텍처

```
Svelte 웹 (:5173) → SvelteKit 서버 프록시 → Rust API 서버 (:8080) → Supabase REST / 외부 API
```

- **Rust API 서버 (Axum :8080)**: 에코 서버 — 포트 호출 + 응답 변환만
- **Svelte 웹 프론트 (SvelteKit :5173)**: UI, API 서버로 프록시
- **Supabase**: DB + Auth (이메일 + Google/Apple 소셜), REST API로 접근
- **Python ML / iOS**: MVP 1 미사용

### 에코 서버 + 포트/어댑터 패턴

모든 앱(서버/웹/iOS)에 동일 패턴 적용:
- 앱은 에코 서버 — 포트 호출 + 응답 변환만
- 모든 외부 호출을 포트(trait/protocol)로 추상화
- State injection으로 프로덕션 어댑터와 Fake 어댑터 교체
- 포트는 관심사별 분리, 어댑터는 통일
- DB 접근은 Supabase REST API (sqlx 미사용)

### 기능 결정

| 항목 | 결정 |
|------|------|
| 핵심 기능 | 키워드/태그 선택 → 자동 웹서치 → LLM 요약+인사이트 |
| 플랫폼 | 웹 (Svelte) 먼저 |
| 서버 | Rust (Axum) API 서버 |
| 수집 소스 | Tavily → Exa → Firecrawl → arXiv (폴백 체인) |
| 본문 추출 | Firecrawl → Tavily Extract → Exa Contents → 스니펫 저장 |
| LLM | OpenRouter + Qwen 3.5 시리즈 |
| DB/Auth | Supabase (이메일 + Google/Apple 소셜) |
| DB 접근 | Supabase REST API (sqlx 미사용) |
| 웹→서버 통신 | SvelteKit 서버 프록시 |
| 화면 | 온보딩(태그) → 피드(매거진) → 상세(요약+인사이트) |
| 화면 확장 | 이후 스크랩 보관함 → 키워드 관리 순으로 추가 |
| 언어 | 다국어 수집 → 한국어 번역/요약 |
| 수집 트리거 | 사용자 새로고침 버튼 |
| UI 스타일 | 매거진 스타일 (썸네일+제목+요약) |
| 배포 | 로컬 Docker + Cloudflare 무료 |

### 경쟁 분석 교훈

- Artifact 실패: 뉴스 소비만으로는 앱 유지 어려움 → 학습 기능 결합 필요
- Feedly AI 유료 기능을 개인 개발자 타겟 무료/저가로 제공하는 포지셔닝

## 서브태스크

| # | 서브태스크 | 의존성 | 상태 |
|---|-----------|--------|------|
| M0 | 프로젝트 부트스트랩 (Rust + Svelte 초기화) | 없음 | 완료 |
| M1 | 인증 + 온보딩 (태그 선택) | M0 | 완료 |
| M2 | 웹서치 수집 파이프라인 (폴백 체인) | M1 | 완료 |
| M3 | LLM 요약 + 인사이트 | M2 | 완료 |
| M4 | 매거진 UI + 상세 페이지 (최소 기능 UI) | M3 | 완료 |
| M5 | 품질 마무리 + 배포 | M4 | 완료 |
| M6 | 배포 파이프라인 (Docker + Cloudflare + iMessage) | M5 | 완료 |

> **디자인 방침**: MVP1은 기능 검증 우선, 최소 Tailwind UI로 진행.
> 전체 UI 디자인 패스(디자인 시스템, 다크 모드, 리디자인)는 MVP2에서 진행.

### 실전 변경 사항 (기획 vs 구현)

| 기획 | 실제 | 이유 |
|------|------|------|
| Supabase REST API (sqlx 미사용) | sqlx 직접 PostgreSQL 연결 | REST API의 복잡한 쿼리 한계, 포트 어댑터만 교체 |
| Qwen 3.5 시리즈 | Qwen3.5-plus (thinking 모드 비활성화) | thinking 모드 활성화 시 JSON 파싱 실패 |
| 단순 배포 | Docker Compose + Cloudflare Tunnel + iMessage 알림 | 실용적 배포 파이프라인 필요성 |

## 워크플로우 설정
### 자동 토론
| 단계 | 상태 |
|------|------|
| step-1 | OFF |
| step-3 | OFF |
| step-5 | OFF |
| step-7 | OFF |

토론 깊이: standard
